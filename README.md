Borrow.py means that when we need to borrow books from library, we need to enroll into it by  our account. If the  informaton is not suitable, we cannot enter the webiste, which means we cannot borrow books, but we do not need to worry it , because website can help us complete the whole registration. It is convenient.
We can use the code in various stages, such as pycharms. When we need to run,we only need to exert enroll_library() method, because if my name cannot exist in the website, enroll_library() will add name in the webiste automated and then run it.At check_name() method, the code will check that whether the came is valid. If teh name is invalid, we can input them again. For _sex__ ()and _years__(), the code  will compare all objects with limited choices. If objects are not related whith these choices, we need to input again.
After comparing with each other, we can certain certification to borrow books. I think it will save lots of time. I remember that workers can help us complete registration by hand_on methods. The conditon will make us waste lots of time. Sometimes, it is possible that they will make mistakes at some details, because they often work carefully for a long time at 
